# Virtual environments
.venv/
venv/

# Training artifacts (too large for GitHub — max ~100 MB per file)
final_model.orbax/
runs/
*.orbax/

# Optional: large local metrics dumps (uncomment if you keep metrics out of repo)
# metrics.json
